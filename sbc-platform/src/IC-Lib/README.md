# Cluster Library Project

## Build

Build tools and dependencies
        
```
sudo apt-get install cmake libuv1-dev
```

- Using build script:
        
        ./build.sh

- Manual build:
               
        Build procedure
             
        ```
        mkdir build
        cd build
        cmake ..
        make
        sudo make install
        sudo ldconfig
        ```
